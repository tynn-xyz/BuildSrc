plugins {
    id 'xyz.tynn.convention.settings' version "$xyzTynnConventionVersion"
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

include ':convention:project'
include ':convention:settings'
